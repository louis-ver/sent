const LOGIN = "LOGIN";
const JOIN = "JOIN";
const PING = "PING";
const LEAVE = "LEAVE";
const ASK = "ASK";
const REFUSE = "REFUSE";
const ACCEPT = "ACCEPT";

module.exports = {
  LOGIN: LOGIN,
  JOIN: JOIN,
  PING: PING,
  LEAVE: LEAVE,
  ASK: ASK,
  REFUSE: REFUSE,
  ACCEPT: ACCEPT
};
